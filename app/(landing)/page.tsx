import React from 'react';

const LandingPage: React.FC = () => {
    // Returned UI
    return (
        <div>
            Landing Page (Unprotected)
        </div>
    );
};

export default LandingPage;
